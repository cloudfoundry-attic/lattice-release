#!/bin/bash

set -e

lattice_release_dir=$(cd `dirname $0` && cd .. && pwd)
vagrant_dir=$lattice_release_dir/vagrant

$lattice_release_dir/scripts/assets $vagrant_dir/assets

pushd "$vagrant_dir" >/dev/null
  packer build $@ lattice.json
popd >/dev/null
