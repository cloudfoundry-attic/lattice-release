---
platform: linux
image: docker:///cloudfoundry/lattice-pipeline
inputs:
- name: lattice
run:
  path: lattice/concourse/tasks/generate-vagrantfile/run.sh
  params:
    S3_LATTICE_PATH: some-s3-lattice-path
