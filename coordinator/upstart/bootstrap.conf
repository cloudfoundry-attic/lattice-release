#!upstart

start on (local-filesystems and net-device-up IFACE!=lo)

pre-start script
    echo "UPSTART: Trying to start lattice bootstrap service - `date +%s%N`"
    /var/lattice/setup/bootstrap
end script
