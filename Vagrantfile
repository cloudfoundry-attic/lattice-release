VAGRANTFILE_API_VERSION = "2"

Vagrant.configure(VAGRANTFILE_API_VERSION) do |config|
  config.vm.box = "diego-edge/diego-lite"
  config.vm.network "private_network", ip: "192.168.11.11"
  config.vm.box_version = '15'
end
